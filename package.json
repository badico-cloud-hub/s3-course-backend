{
  "name": "badico-s3-backend",
  "version": "1.0.0",
  "description": "a simple backend for the s3 badico course",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index",
    "start.dev": "nodemon -L",
    "start.local": "STAGE=dev nodemon -L -r dotenv/config index.js dotenv_config_path=.env"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "author": "",
  "license": "ISC"
}
