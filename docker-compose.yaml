version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/mode_modules
    ports:
      - "8000:8080"
    environment:
      - STAGE=dev 
      - PORT=8080
